# Instructions

```bash
find <opentitan_path> -name \*_reg_top.sv > reg_top.txt # this searchs for all reg_top files
python3 scanner1.py > scanner_res.txt # this runs the scanner for every file in reg_top.txt
```